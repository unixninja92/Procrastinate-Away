// Generated by CoffeeScript 1.9.1
(function() {
  this.Blocker = (function() {
    function Blocker() {
      this.loggedIn = false;
      this.isWhitelist = false;
      this.list = ['cnn.com'];
      this.areBlocking = false;
    }

    Blocker.prototype.getList = function() {
      return this.list;
    };

    Blocker.prototype.parseJson = function(json) {};

    Blocker.prototype.isWhitelist = function() {
      return this.isWhitelist;
    };

    Blocker.prototype.blocked = function(id, url) {};

    Blocker.prototype.isBlocked = function(url, regex, time) {};

    Blocker.prototype.checkUrl = function(id, url, testmode) {
      var bool;
      if (this.list != null) {
        bool = this.list.indexOf(url) !== -1;
        if (this.isWhitelist) {
          return !bool;
        } else {
          return bool;
        }
      }
    };

    Blocker.prototype.run = function(tab) {
      if (!this.areBlocking) {
        return false;
      }
      return checkUrl(tab.id, tab.url, false);
    };

    return Blocker;

  })();

}).call(this);
