// Generated by CoffeeScript 1.9.1
(function() {
  var blocker;

  blocker = getBlocker();

  chrome.tabs.query({
    'active': true,
    'lastFocusedWindow': true
  }, function(tab) {
    var url;
    url = tab[0].url;
    if ((tab[0] != null) && (url != null)) {
      $('#block-np').hide();
      if (blocker.checkUrl(tab[0].id, url, true)) {
        return $('#block-blocked').show();
      } else {
        return $('#block-not-blocked').show();
      }
    }
  });

}).call(this);
